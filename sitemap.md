# Sitemap

    pi5-homelab/
    ├── README.md               # project summary, hardware, architecture snapshot, progress
    ├── sitemap.md              # this file — map of the repo
    ├── LICENSE                 # MIT license
    ├── .gitignore              # keeps secrets / un-sanitized configs out of git
    ├── docs/
    │   ├── architecture.md     # network topology, interface map, design decisions
    │   ├── services.md         # each service: interface, ports, config paths
    │   ├── lessons-learned.md  # mistakes, fixes, and gotchas (running log)
    │   └── setup/
    │       ├── 00-github.md    # Step 0 — repo conventions + setup record
    │       ├── 01-assembly.md  # Step 1 — physical assembly  (placeholder)
    │       └── ...             # 02-… through 09-…, one file per step
    ├── configs/                # sanitized config files
    │   └── configplaceholderfile   # temporary — delete when real configs land
    └── scripts/                # helper scripts
        └── scptplaceholder         # temporary — delete when real scripts land
